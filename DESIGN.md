# Jia him 工作台视觉设计系统

| 项目 | 内容 |
| --- | --- |
| 状态 | Active |
| 版本 | v1.1 |
| 日期 | 2026-09-06 |
| 适用范围 | `apps/admin` 本地内容工作台 |

## 1. 设计定位

Jia him 管理端是一套 **Warm Editorial Workbench**：它保留个人内容站的温暖纸张感，但首先是一件高效、安静、可长时间使用的桌面编辑工具。

界面以内容为主，结构来自对齐、字号、字重、表面层级和 1px 分隔线。固定工作区不依赖浮动卡片、大片留白或装饰性阴影建立层级。每个区域只能有一个主要任务，空白区域和辅助信息不得压过正在编辑的内容。

## 2. 基本原则

1. **内容优先**：文章、源码和预览比品牌装饰、状态说明及空状态更重要。
2. **高密度但可读**：桌面列表优先缩减间距，再调整字号；有意义的 UI 文本不得低于 12px，极短机器标签可使用 11px。
3. **固定面板扁平化**：侧栏、编辑器、预览和工具栏边到边衔接，以表面色和 1px 分隔线分区，不使用卡片阴影。
4. **状态只出现一次**：同一分支、修改数、模式或保存状态不能同时出现在顶栏、正文和底栏；重复信息必须删除或转为按需查看。
5. **克制的强调**：深绿色只用于主要操作、当前选择、焦点和少量品牌识别；成功、警告、错误颜色只表达对应语义。
6. **动作解释状态**：动效仅用于 hover、选择、展开收起、加载和浮层出现，不承担装饰作用。

## 3. 颜色语义

| Token | 值 | 用途 |
| --- | --- | --- |
| `paper` | `#f3efe6` | 侧栏与温暖底色 |
| `paper-deep` | `#e8e1d3` | 次级选中或层次背景 |
| `surface` | `#fbfaf6` | 编辑器、预览和主要工作面 |
| `ink` | `#1f2923` | 标题与主要文本 |
| `muted` | `#5f6861` | 日期、说明和次要信息；在最深的工作台底色上仍满足普通文本对比度 |
| `line` | `rgba(31, 41, 35, .13)` | 固定面板和行分隔线 |
| `forest` | `#285443` | 主要操作、选中和焦点 |
| `amber` | `#a35d23` | 警告或待处理状态 |
| `danger` | `#9b352f` | 错误和破坏性操作 |

禁止为普通面板、栏目或层级临时发明新颜色。固定区域使用表面差异和细线分隔；阴影只属于弹窗、菜单、tooltip 等真正悬浮的界面。

登录页是品牌入口而非固定工作区，允许使用一次克制的纸张色渐变和居中浮层；该例外不得扩散到文章、设置、Git 或预览工作区。

## 4. 字体与层级

管理界面的中英文使用同一套无衬线字体栈：`Noto Sans SC`、`Noto Sans CJK SC`、`Source Han Sans SC`、`PingFang SC`、系统中文字体。代码、路径和 Git 标识才使用等宽字体。文章预览可保留独立的阅读排版，但控件、导航和元数据不得混用字体。

`html` 保持 `font-size: 100%`，尊重浏览器和用户的根字号偏好；不要把根字号写死为 14px 或 16px。Admin 常规正文、标签和输入控件以 `.875rem` 为默认值（浏览器默认设置下为 14px），其余尺寸使用 rem 语义 token：

| 角色 | 字号 | 建议行高 | 用途 |
| --- | --- | --- | --- |
| 工作区标题 | 20–24px / `1.25–1.5rem` | 1.25 | 页面和一级工作区标题；设置分组默认 20px |
| 小标题 | 16–18px / `1–1.125rem` | 1.3 | 对话框或独立区块标题 |
| Admin 正文、控件与标签 | 14px / `.875rem` | 1.45 | 表单、按钮、设置导航、编辑元数据 |
| 阅读预览正文 | 16px / `1rem` | 1.7 | 文章预览默认值，独立于 Admin 密度 |
| 紧凑辅助控件 | 13px / `.8125rem` | 1.35 | 标签页、密度较高的辅助动作 |
| 元数据 | 12px / `.75rem` | 1.35 | 日期、计数、帮助文本 |
| 机器标签 | 11px / `.6875rem` | 1.3 | 极短状态徽标，不用于正文 |

层级优先通过字重建立：正文 400，标签 500，当前选择和区块标题 600，主要操作最高 700。普通列表项不得全部使用粗体。

编辑器与阅读预览有两组互不干扰、保存在浏览器本地的字号偏好：编辑器为 13/14/16px、默认 14px；阅读预览为 14/16/18px、默认 16px。控制器只改变对应工作面，不修改站点正文 CSS，也不通过改变 `html` 根字号连带缩放整个产品。

## 5. 间距、形状与深度

基础间距为 4px，合法序列为 4、8、12、16、24、32、48px。相关字段使用 4–8px，控件组使用 8–12px，独立区域使用 16–24px。工作台中不得用 32px 以上留白代替结构设计。

- 固定面板圆角为 0。
- 行、输入框和小按钮使用 6px。
- 搜索框、分段控件和独立控件使用 8px。
- 弹窗和浮层最多使用 12px。
- 完全圆形只用于头像、计数和明确的胶囊状态；普通按钮不默认使用药丸形。
- 固定面板、列表行和工具栏无阴影；浮层使用单一轻阴影。

## 6. 工作区布局与密度

宽屏保留左侧文章列表、中间源码、右侧预览三栏。中间编辑区必须保持主要视觉权重，侧栏在空间不足时先收缩或折叠。

桌面三栏之间使用类似 VS Code 的垂直分隔器：视觉线保持 1px，命中区扩大到 6px。文章列表宽度可在 220–420px 之间调整；编辑区与预览区分别保留至少 420px 和 320px。拖动过程中即时反馈且不做缓动，松手后的宽度与比例写入本地存储；刷新或重新展开侧栏时恢复。分隔器支持左右方向键微调，双击任一分隔器恢复默认布局。

- 顶栏目标高度：44–56px。
- 面板工具栏目标高度：40–48px。
- 紧凑文章行目标高度：42–48px；标题单行省略，完整文本通过原生 title 或 tooltip 查看。
- 桌面输入控件目标高度：32–36px。
- 元数据采用单行横向栏：短字段固定宽度，摘要占剩余空间。
- 各面板独立滚动，工具栏和当前上下文保持可见。
- 侧栏收起后成为贴左边界的窄分隔轨道；展开按钮位于轨道右侧，不保留快捷栏宽度。
- 不设置常驻底部状态栏；快捷键和 Git 详情按上下文展示。

当前断点行为：

- `> 1180px`：完整三栏。
- `981–1180px`：紧凑三栏，压缩侧栏和辅助操作。
- `761–980px`：文章列表保持固定，编辑器和预览通过工作区顶部的「编辑 / 预览」分段控件切换。
- `≤ 760px`：编辑器或预览占满工作区；「文章」按钮打开复用现有列表的左侧抽屉。

窄屏抽屉覆盖当前内容而不挤压编辑器。点击遮罩、按 Escape、新建文章或选择文章都会关闭抽屉；选择或新建后回到编辑视图。桌面侧栏折叠偏好不影响移动抽屉的完整内容，窗口重新变宽时抽屉自动关闭并恢复原偏好。

## 7. 组件规则

### 文章列表

栏目过滤使用单个下拉选择器并显示数量，避免栏目数增长时占满侧栏。文章标题和日期构成两层层级。文章标题 14px，日期与分类 11–12px；行内垂直间距保持紧凑。选中态使用轻表面变化和细边框，不通过大阴影或位移制造卡片感。

### 编辑元数据

发布日期与摘要位于标题下方的一条紧凑栏内。标签和输入框同行，日期字段不抢占宽度，摘要输入自动填充。该区域服务于快速编辑，不做成独立表单卡片。

### 状态与反馈

分支和修改数保留在顶栏；错误、恢复提示和保存反馈出现在与当前操作相邻的位置。禁用动作必须提供当前可见、可执行的原因，不能只依赖 hover tooltip；设置发布状态按“先保存 → 仓库/分支/会话门禁 → 可发布文件”顺序给出下一步，并在窄屏自然换行。

### 按钮和标签页

每个视图最多一个高强调主要操作。次要操作使用透明或浅色表面。hover 改变背景或边框，pressed 可轻微压暗；不能同时使用位移、缩放、阴影和颜色变化。

## 8. 动效与无障碍

- hover 与 pressed：120–160ms。
- 面板展开、收起和浮层：160–200ms。
- 使用标准 ease 或轻微 ease-out，禁止弹簧、回弹和装饰性循环动画。
- `prefers-reduced-motion` 下取消非必要动画。
- 所有键盘操作必须有清晰焦点环；颜色不能成为唯一状态信号。
- 触控环境保持至少 44px 命中区域，桌面紧凑控件可缩至 30–32px。

## 9. 禁止项

- 禁止玻璃拟态、霓虹、装饰渐变、浮动光斑和无功能插画。
- 禁止固定工作区卡片化、处处药丸按钮、重复状态栏和过度圆角。
- 禁止为解决密度问题把所有文字缩到 12px 以下。
- 禁止标题、正文、按钮和标签随意使用不同字体。
- 禁止仅凭单张截图新增尺寸；重要布局变更需要在宽屏、紧凑桌面和窄屏验证。

## 10. 响应式验收基线

- 宽屏验收使用 1098px，确认响应式控制栏隐藏且三栏同时存在。
- 紧凑验收使用 980px，确认固定文章列表和编辑/预览切换可用。
- 窄屏验收使用 760px 与 560px，确认抽屉、遮罩、Escape、文章选择和 44px 触控目标可用。
- 所有基线都必须无页面级横向滚动；`prefers-reduced-motion` 下取消抽屉与遮罩过渡。

## 11. 决策记录

- 2026-09-02：采用 Warm Editorial Workbench；保留米白与森林绿品牌语言，引入紧凑桌面工具密度。
- 2026-09-02：固定面板改为边到边、无阴影；移除重复底部状态栏。
- 2026-09-02：文章列表改为单行紧凑标题加完整标题 hover；元数据改为单行横向栏。
- 2026-09-02：980px 以下增加编辑/预览切换，760px 以下将唯一文章列表转换为可键盘关闭的左侧抽屉。
- 2026-09-06：`html` 改为用户可调的 `100%` 根字号；Admin 默认正文与控件统一为 `.875rem`，阅读预览保留 16px 默认，并提供独立的 14/16/18px 调节。
- 2026-09-06：栏目过滤改为带计数的下拉框；编辑器与预览字号偏好分别持久化，不改变站点内容样式。
- 2026-09-06：设置发布禁用原因由 tooltip 升级为工作区内常驻状态，精确说明“先保存”或当前分支门禁。
- 2026-09-06：栏目导航选择器只列出尚未进入导航的启用顶级栏目；全部占用时禁用入口并常驻解释“删除后可重新添加、仅隐藏不会释放”。多语言站点从第一个仍有候选项的启用语言打开，避免出现可打开但内容为空的对话框。
- 2026-09-06：shadcn 按钮前景色必须由 variant 的语义 Token 决定；未分层的全局元素重置不得声明 `button { color: inherit }` 覆盖 Tailwind utility。浅色主按钮使用森林绿配白字，深色主按钮使用浅鼠尾草绿配深墨绿字。
